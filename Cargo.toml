[package]
name = "iceland"
version = "0.1.0"
edition = "2021"
description = "A CLI tool to create and manage focused digital areas"

[dependencies]
clap = { version = "4.4", features = ["derive"] }
dirs = "5.0"